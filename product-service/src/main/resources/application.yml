server:
  port: 8100
  servlet:
    context-path: /product-service

spring:
  application:
    name: product-service
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/