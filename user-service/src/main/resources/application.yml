server:
  port: 8200
  servlet:
    context-path: /user-service

spring:
  application:
    name: user-service
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/